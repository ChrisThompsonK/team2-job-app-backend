# Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file to version control!

# ===========================================
# Application Settings
# ===========================================

# Application name
APP_NAME=team2-job-app-backend

# Application version
APP_VERSION=1.0.0

# Node environment: development, production, or test
NODE_ENV=development

# ===========================================
# Server Configuration
# ===========================================

# Port number the server will listen on
PORT=8000

# Host address for the server
HOST=localhost

# Frontend URL for CORS configuration
# Used to allow requests from the frontend application
# Default: http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Path to SQLite database file
DATABASE_URL=./database.sqlite

# ===========================================
# Feature Flags
# ===========================================

# Enable debug mode (true/false)
DEBUG=false

# ===========================================
# Session Configuration
# ===========================================
# Sessions are stored in the main database.sqlite file in the 'sessions' table

# Session secret (min 32 characters, generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your-super-secret-key-min-32-chars-long-change-this

# Session cookie name
SESSION_NAME=job_app_session

# Session max age in milliseconds (default: 7 days)
SESSION_MAX_AGE=604800000

# ===========================================
# Database Seeding (Optional)
# ===========================================

# Admin user password for database seeding
# If not set, uses default development password (not recommended for production)
# Generate a strong password and set it here for production
# SEED_ADMIN_PASSWORD=change-me-to-secure-password

# Applicant users password for database seeding
# If not set, uses default development password (not recommended for production)
# Generate a strong password and set it here for production
# SEED_APPLICANT_PASSWORD=change-me-to-secure-password

# ===========================================
# Notes
# ===========================================
# - All values are strings, even numbers
# - Boolean values should be 'true' or 'false'
# - Paths can be relative or absolute
# - Required variables: (none for local development)
# - For production, use platform-specific environment variable tools
