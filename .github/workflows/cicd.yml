name: CI/CD Pipeline

on:
  pull_request:
    branches: ["*"]
  push:
    branches: ["*"]
  workflow_dispatch:

env:
  NODE_VERSION: "20"
  CONTAINER_PORT: "8000"
  CONTAINER_STARTUP_TIMEOUT: "30"
  AZURE_ACR_NAME: "aiacademy25"
  AZURE_ACR_SERVER: "aiacademy25-bbaue6bgenhkd0dj.azurecr.io"
  CONTAINER_IMAGE_NAME: "team2-job-app-backend"

jobs:
  # Stage 1: Code Quality Checks
  code-quality:
    name: Code Quality & Linting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run Biome check
        run: npm run check

      - name: Run TypeScript type check
        run: npm run type-check

  # Stage 2: Testing
  test:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest
    needs: code-quality

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test:run

      - name: Run tests with coverage
        run: npm run test:coverage

  # Stage 3: Build Application
  build-app:
    name: Build Application
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build


  # Stage 4: Build Docker Image
  build-docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: build-app
    outputs:
      image-tag: ${{ steps.meta.outputs.image-tag }}
      short-sha: ${{ steps.meta.outputs.short_sha }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate image metadata
        id: meta
        run: |
          VERSION=$(node -p "require('./package.json').version")
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          BRANCH_NAME=$(echo ${{ github.ref_name }} | sed 's/[^a-zA-Z0-9]/-/g')
          
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            IMAGE_TAG="pr-${{ github.event.pull_request.number }}"
          else
            IMAGE_TAG="$VERSION"
          fi
          
          # Create tags for local testing
          TAGS="team2-job-app-backend:${SHORT_SHA}"
          TAGS="${TAGS},team2-job-app-backend:${BRANCH_NAME}"
          
          if [ "${{ github.ref_name }}" = "main" ]; then
            TAGS="${TAGS},team2-job-app-backend:latest"
          fi
          
          echo "image-tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "short_sha=$SHORT_SHA" >> $GITHUB_OUTPUT
          echo "tags=$TAGS" >> $GITHUB_OUTPUT

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: true

      - name: Display build information
        run: |
          echo "✅ Docker image built successfully!"
          echo ""
          echo "Image tags:"
          echo "${{ steps.meta.outputs.tags }}" | tr ',' '\n' | sed 's/^/  - /'
          echo ""
          echo "Git SHA: ${{ github.sha }}"
          echo "Short SHA: ${{ steps.meta.outputs.short_sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Build time: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

      - name: Test container startup
        run: |
          IMAGE_TAG="team2-job-app-backend:${{ steps.meta.outputs.short_sha }}"
          echo "Testing image: ${IMAGE_TAG}"
          
          docker run -d --name test-container -p ${{ env.CONTAINER_PORT }}:${{ env.CONTAINER_PORT }} ${IMAGE_TAG}
          
          echo "Waiting for container to start..."
          READY=false
          for i in $(seq 1 ${{ env.CONTAINER_STARTUP_TIMEOUT }}); do
            if docker ps | grep -q test-container; then
              if curl -sf http://localhost:${{ env.CONTAINER_PORT }}/ > /dev/null 2>&1; then
                echo "✅ Container is running and application is responding"
                READY=true
                break
              fi
            fi
            sleep 1
          done
          
          if [ "$READY" = false ]; then
            echo "❌ Container is running but application not responding after ${{ env.CONTAINER_STARTUP_TIMEOUT }} seconds"
            docker logs test-container
            exit 1
          fi
          
          if docker ps | grep -q test-container; then
            echo "✅ Container started successfully"
            docker logs test-container
          else
            echo "❌ Container failed to start or crashed"
            docker logs test-container
            exit 1
          fi
          
          docker stop test-container
          docker rm test-container

  # Stage 5: Deploy to Azure Container Registry (main branch only)
  deploy-to-acr:
    name: Push to Azure Container Registry
    runs-on: ubuntu-latest
    needs: build-docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Log in to Azure Container Registry
        run: az acr login --name ${{ env.AZURE_ACR_NAME }}

      - name: Build and push Docker image to ACR
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.AZURE_ACR_SERVER }}/${{ env.CONTAINER_IMAGE_NAME }}:${{ needs.build-docker.outputs.image-tag }}
            ${{ env.AZURE_ACR_SERVER }}/${{ env.CONTAINER_IMAGE_NAME }}:latest
          cache-from: type=registry,ref=${{ env.AZURE_ACR_SERVER }}/${{ env.CONTAINER_IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.AZURE_ACR_SERVER }}/${{ env.CONTAINER_IMAGE_NAME }}:buildcache,mode=max

      - name: Verify image in ACR
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            az acr repository show-tags --name ${{ env.AZURE_ACR_NAME }} --repository ${{ env.CONTAINER_IMAGE_NAME }}

  # Stage 6: Terraform Infrastructure Deployment
  terraform-deploy:
    name: Terraform Deployment
    runs-on: ubuntu-latest
    needs: deploy-to-acr
    if: github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '~> 1.5'

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set ARM environment variables
        run: |
          echo "ARM_CLIENT_ID=$(jq -r .clientId <<< '${{ secrets.AZURE_CREDENTIALS }}')" >> $GITHUB_ENV
          echo "ARM_CLIENT_SECRET=$(jq -r .clientSecret <<< '${{ secrets.AZURE_CREDENTIALS }}')" >> $GITHUB_ENV
          echo "ARM_SUBSCRIPTION_ID=$(jq -r .subscriptionId <<< '${{ secrets.AZURE_CREDENTIALS }}')" >> $GITHUB_ENV
          echo "ARM_TENANT_ID=$(jq -r .tenantId <<< '${{ secrets.AZURE_CREDENTIALS }}')" >> $GITHUB_ENV

      - name: Terraform Init
        working-directory: ./infrastructure
        run: |
          terraform init \

      - name: Terraform Validate
        working-directory: ./infrastructure
        run: terraform validate

      - name: Terraform Plan
        working-directory: ./infrastructure
        run: terraform plan -var-file="dev.tfvars" -out=tfplan

      - name: Terraform Apply (main branch only)
        if: github.ref == 'refs/heads/main'
        working-directory: ./infrastructure
        run: terraform apply -auto-approve tfplan

      - name: Terraform Output
        working-directory: ./infrastructure
        run: terraform output -json

